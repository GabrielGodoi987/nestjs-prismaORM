import { NotfounderrorfilterFilter } from './notfounderrorfilter.filter';

describe('NotfounderrorfilterFilter', () => {
  it('should be defined', () => {
    expect(new NotfounderrorfilterFilter()).toBeDefined();
  });
});
